import { createTheme } from "@mantine/core";

export const theme = createTheme({
    primaryColor: "blue",
    defaultRadius: "sm",
    cursorType: "pointer",
    headings: {
        fontWeight: "600",
    },
});
